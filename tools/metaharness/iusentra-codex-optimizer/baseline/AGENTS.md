# Project Instructions

- Read the repository before editing.
- Keep changes inside the declared IUSENTRA scope.
- Never use destructive git commands such as `git reset --hard` or `git checkout --`.
- Do not change runtime dependencies, product UI, storage, Lex AI, telematic portals, Docker or Railway for harness-only tasks.
- Run the relevant IUSENTRA quality gate before reporting completion.
