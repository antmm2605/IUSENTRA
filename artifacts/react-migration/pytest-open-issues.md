# Pytest issue aperte e risoluzioni

Aggiornato: 2026-05-13, Audit probatorio WORM 2.221.0.

## Regola operativa

Questo file e' la coda di lavoro. Prima di rilanciare test gia' passati, controllare qui: si lavora solo sugli item aperti, poi si spostano nel report `pytest-confirmed-ok.md` quando il test o lo shard passa.

## Issue aperte

| Area | Test / comando | Stato | Problema rilevato | Risoluzione |
| --- | --- | --- | --- | --- |
| PST / prompt PIN ripetuti in acquisizione React | Shard React/Local Signer/PST e build Vite | Nessuna issue aperta sul codice | Dopo la correzione 2.216.3-2.216.4 il flusso poteva perdere la sessione `view` se lo stato React in memoria non era allineato al payload gia' ricevuto da ricerca/anteprima; il download poteva quindi aprire una nuova sessione e aumentare le richieste PIN. | `TelematicoSurfacePage` recupera la sessione PST anche da `selection.raw.pst_session` e `preview.pst_session`, la conserva nello storage sessione e invia lo stesso `pst_session_id` a `/pst/download-documenti-batch`. Il wizard accetta anche `fascicolo_id`/`target_fascicolo_id`. Gate mirati e build registrati in `pytest-confirmed-ok.md`. |
| Audit probatorio WORM 2.221.0 | Test audit mirati, packaging e build React | Nessuna issue aperta sul codice | I giri iniziali hanno evidenziato problemi mirati: helper test non importabile come package, aspettativa di ordine emissione nello stesso secondo, ordinamento catena ambiguo con timestamp al secondo + UUID v4, snapshot job inizialmente tenant-esplicito, adapter CAdES da rendere operativo verso Local Signer, fallback tenant `default` da rimuovere su route/hook e guardia esplicita sull'assenza API delete WORM. | Aggiunto `tests/__init__.py`, import helper come `tests.audit_test_utils`, confronto per insieme di kind, timestamp evento a microsecondi e UUIDv7 quando disponibile, discovery tenant con eventi aperti, client CAdES HTTP configurabile, route e hook fail-closed senza tenant e test anti-delete; test audit 30/30, compileall, packaging/readiness, contratti React, typecheck e build Vite verdi. |
| Audit gate React reale 2.220.0 | Gate React, audit anti-mascheramento, `tests/test_react_shell.py` | Nessuna issue aperta sul codice | I gate hanno evidenziato contraddizioni reali: `/sito-studio/builder` era nel manifest ma bloccato dal gate, lo scadenziario profondo non era protetto chirurgicamente, `Template Atti` conteneva form HTML nel componente full e la dashboard esponeva un nome fallback con `mock`. | Allineati gate/shell/manifest/script, convertiti i controlli Template Atti a submit React JSON, rinominato fallback dashboard, aggiunti contratti legacy per route ad alto rischio lasciate legacy-first. Tutti i gate richiesti sono verdi e registrati in `pytest-confirmed-ok.md`. |
| Portali non-PST assistiti 2.219.0 | `tests/test_portali_payload_import_ui.py`, `tests/test_pdp_penale_web.py`, `tests/test_deposito_guidato.py` | Nessuna issue aperta sul codice | Durante la verifica mirata sono emersi solo due riallineamenti: la microcopy storica PDP era nascosta dal nuovo pannello assistito e il test PDP con data 2026-05-13 usciva dalla finestra PEC di 30 giorni. | Ripristinata la microcopy del workflow PDP dentro il pannello assistito e allargata la finestra di associazione PEC PDP a 60 giorni; py_compile e shard mirati verdi. |

| Area | Test / comando | Stato | Problema rilevato | Risoluzione |
| --- | --- | --- | --- | --- |
| Email ordinaria accenti 2.218.9 | Corpo/oggetto con `possibilit�` invece di `possibilità` | Nessuna issue aperta sul codice | Alcuni messaggi arrivano con byte Windows-1252/Latin-1 ma dichiarano `utf-8`; il parser usava `errors="replace"` e salvava `�` invece di provare una decodifica alternativa. | Decoder robusto per header/corpo con scoring anti-`�` e anti-mojibake; sync IMAP include i record gia' salvati con `�` nella finestra di riparazione e aggiorna i testi se il messaggio sorgente e' disponibile. Test mirati e py_compile verdi. |
| Email ordinaria duplicati inviati 2.218.8 | Lista Email ordinaria con doppia riga locale/IMAP per alcuni studi | Nessuna issue aperta sul codice | Alcuni provider salvano nella cartella Inviati una copia IMAP con orario diverso dalla copia locale registrata dall'app; senza `Message-ID` stabile il confronto precedente pretendeva lo stesso secondo e lasciava due righe. | Gli invii SMTP generano sempre `Message-ID`; `sincronizza_inviati` deduplica la coppia locale/IMAP anche con scarto orario se oggetto, destinatari e corpo coincidono, mantenendo la guardia che non fonde due invii locali simili. Test mirati, py_compile, typecheck, contratti React, packaging, release readiness, build e Docker locale verdi. |
| Email PEC allegati `postacert.eml` 2.218.7 | URL `/email/messaggio/<id>/allegato/0` e dettaglio React PEC | Nessuna issue aperta sul codice; sorgente PEC storica assente per il messaggio segnalato | I messaggi PEC storici potevano avere `postacert.eml` registrato come metadato `message/rfc822` senza file fisico; alcuni server espongono inoltre il nome file nel `Content-Type` senza `Content-Disposition`, causando 404 sul link diretto e azioni React verso un allegato non recuperato. Legalmail espone anche cartelle non quotate (`INBOX.Cestino`, `INBOX.Inviata`) che prima non venivano scoperte. | Parser IMAP aggiornato per salvare `message/rfc822` anche quando il nome file e' solo nel `Content-Type`; bridge React con azioni solo se il file esiste; route PEC/ordinaria con risposta controllata e istruzione non ciclica; parser cartelle Legalmail corretto. Il messaggio `c05849df94244c9a946813566d5a3934` non e' piu' presente sul server PEC con il vecchio UID ne' nelle cartelle disponibili, quindi non viene creato un `postacert.eml` sostitutivo. |
| Template Atti Cartabia / prefill / timbro studio 2.218.0 | Gate richiesti dal prompt | Nessuna issue aperta | Durante lo sviluppo sono emersi solo tre problemi locali: fixture API timbro con username gia' presente, contratto React che rifiutava stili inline nella preview timbro e un titolo pratica storico con parola `demo` visibile nel compilatore. | Fixture rinominata, preview spostata su classi CSS governate e filtro UI applicato solo alla visualizzazione dei dati storici; script catalogo, py_compile, pytest mirati, typecheck, contratti React, build Vite, Docker locale, smoke browser, packaging e readiness release sono verdi. |
| PWA/Web Push notifiche dispositivo 2.217.2 | Gate richiesti dal prompt | Nessuna issue aperta | Il primo typecheck ha segnalato solo il tipo della chiave VAPID in `applicationServerKey`, perche' `Uint8Array<ArrayBufferLike>` non era accettato dal DOM lib corrente. | Convertita esplicitamente la chiave pubblica in `ArrayBuffer`; typecheck, build, test backend mirati, packaging e readiness sono verdi. Nessuna chiamata di rete reale verso servizi push. |
| Notifiche legali sicure 2.217.1 | Gate richiesti dal prompt | Nessuna issue aperta | Durante il primo smoke visuale il catalogo cliente esponeva `clienti-2026.05.12`, versione distinta ma troppo simile alla versione relata vietata nel tab cliente. | Rinominata la versione cliente in `comunicazioni-cliente-1.0`; test mirati, typecheck, contratti, build, packaging, Docker no-cache e smoke browser caldo desktop/tablet/mobile tutti verdi. |
| Calendar Sync demo persistente | `python tools/demo_calendar_sync.py` ripetuto su dati gia' presenti | Risolto e verificato in 2.217.0 | La prima versione della demo poteva creare un appuntamento sullo stesso slot usato da una prova precedente e incorrere nella guardia sovrapposizioni dell'Agenda. | La demo cerca automaticamente il primo slot libero a step di 75 minuti, preservando la persistenza locale e restando ripetibile; comando rilanciato e completato con tutti gli `[OK]`. |
| React contratti Calendari | `npm --prefix frontend run test` | Risolto e verificato in 2.217.0 | Il contratto statico storico cercava ancora il marker `createCalendarProfile` nel pannello calendario, mentre il nuovo flusso WebCal passa dal motore bidirezionale. | Mantenuto il presidio compatibile nel componente e rilanciati `npm --prefix frontend run test` e `npm --prefix frontend run typecheck`, entrambi verdi. |
| Codex Harness / file release protetti | `python tools/codex_harness/run_codex_quality_gate.py --mode code` | Non applicabile come blocco di rilascio 2.216.7 | Il gate ha segnalato modifiche a file protetti (`Dockerfile`, `pct/__init__.py`, `railway.toml`, `setup.py`) e dipendenza runtime perche' questa tranche richiede il bump versione e l'allineamento packaging/deploy. AGENTS guardrails e Open Design support sono passati. | Esito sostituito dai gate specifici di release gia' verdi: `python tools/sync_packaging_files.py --check`, `tests/test_packaging_consistency.py`, `tests/test_release_readiness.py`, build Vite, Docker no-cache, readiness locale e browser reale. Non rilanciare il profilo finche' i file versione/deploy restano intenzionalmente modificati. |
| Notifiche legali L. 53 | `tests/test_notifiche_legali.py::test_api_react_notifiche_legali_espone_workflow_separati` | Risolto e verificato in 2.216.7 | Il bridge usava `datetime.utcnow()`, trattato come warning bloccante in Python 3.14 durante il test API. | Sostituito con `datetime.now(timezone.utc)` e formato ISO UTC stabile; shard `tests/test_notifiche_legali.py` verde. |
| React shell / notifiche legali | `tests/test_react_shell.py::test_route_ufficiali_email_messaggi_servono_react_con_vista_classica_tecnica` | Risolto e verificato in 2.216.7 | Il test pretendeva una vista classica `?_legacy=1` per `/notifiche-legali`, ma la nuova route nasce direttamente React e non ha una pagina legacy equivalente. | Rimosso l'assunto sul fallback classico per questa route; la route ufficiale resta servita dalla shell React e lo shard comunicazioni e' verde. |
| React Fascicoli / Fascicolo Veloce guidato | `tests/test_react_shell.py::test_post_nuovo_fascicolo_veloce_restituisce_errori_chiari_json` | Risolto e verificato in 2.216.5 | Il form poteva mostrare un errore generico quando il fascicolo veloce mancava di dati bloccanti, e non apriva automaticamente il deposito dopo la creazione. | API form arricchita con clienti/soggetti/uffici reali; POST valida autorita', controparte e identificativo, restituisce messaggi JSON espliciti e reindirizza a `/fascicoli/<id>/deposito/prepara` dopo la creazione veloce. |
| Gate Codex Harness dev-tooling | `python tools/codex_harness/run_codex_quality_gate.py --mode dev-tooling` | Non applicabile al perimetro, sostituito da gate `--mode code` verde | Il modo `dev-tooling` blocca la worktree per file runtime `data/` gia' sporchi e fuori perimetro tool; nessuno di quei file e' stato staged o committato. | Per questa tranche applicativa e' stato eseguito `python tools/codex_harness/run_codex_quality_gate.py --mode code`, confermato verde e documentato in `pytest-confirmed-ok.md`. |
| PST / documento catalogato ma non scaricato | Browser reale Palmi RG 274/2026, download batch | Risolto e verificato in 2.216.4 | Il catalogo PST espone `AttoNonCodificato_34341272.pdf` con `idCat 34341272`, ma il download batch ha restituito 5/6 file: per quell'atto il PST non ha restituito contenuto. La policy precedente interrompeva l'importazione anche quando il fascicolo locale era gia' presente. | Il wizard consente il download parziale solo se viene selezionato `Collega a pratica esistente` o `Aggiorna pratica esistente`: importa i file ricevuti, conserva il documento non restituito come voce catalogata da acquisire e mantiene gli identificativi portale piu' ricchi nel merge. Traccia locale ignorata dal git: `artifacts/pst-import-traces/palmi-274-2026-20260511-165722.local.*`. |
| PST / prompt PIN ripetuti | Wizard acquisizione PST e Local Signer batch | Risolto e verificato in 2.216.4 | La sessione `view/import` era gia' unificata in 2.216.3, ma la prova reale su Palmi RG 274/2026 ha mostrato tre prompt in consultazione perche' il preflight in timeout veniva marcato `auth_ready` e il PST rifiutava poi il cookie-only, forzando nuovi round mTLS in processi `curl` separati. | Local Signer 1.6.28 non considera autenticato un preflight senza HTTP reale; il nuovo `/pst/ricerca-snapshot` accorpa ricerca e catalogo documenti in un solo processo `curl` per le ricerche esatte non SIGP; wizard Flask e React riusano lo snapshot e i download restano solo batch con lo stesso `pst_session_id`. Test mirati Local Signer/PST, build React e scansione anti-chiamate dirette verdi. |
| React Fascicoli / Fascicolo Veloce | `tests/test_react_shell.py::test_post_nuovo_fascicolo_veloce_carica_documenti_ed_email_eml` | Risolto e verificato in 2.216.0 | Durante lo sviluppo il test iniziale leggeva direttamente il JSON root e poi il dettaglio API non esponeva ancora i conteggi `fascicoloVeloce`, `documentiInizialiCount` ed `emailInizialiCount`. | Test corretto sul dettaglio API tenant-aware e bridge aggiornato; shard mirato 2/2 verde, typecheck, contratti React e build Vite verdi. |
| Codex harness / worktree runtime locale | `python tools/codex_harness/run_codex_quality_gate.py --mode dev-tooling` | Isolato, non bloccante per deploy applicativo | Il profilo `dev-tooling` ha fallito solo per file runtime locali in `data/` gia' non staged e fuori dal perimetro strumenti; non ha segnalato problemi su dipendenze runtime, guardrail AGENTS o Open Design support. | Eseguito il profilo coerente con la tranche applicativa: `python tools/codex_harness/run_codex_quality_gate.py --mode code`, verde. I file `data/` restano non committati. |
| Docker locale / primo warm-up tenant | Browser Playwright autenticato su `/documenti` 2.215.7 e `/fascicoli/nuovo` 2.216.0 subito dopo rebuild container | Aperto, da trattare in tranche performance separata | La prima richiesta tenant autenticata dopo riavvio container puo' impiegare circa 60-150s prima di servire la shell; le richieste successive sono rapide. Il comportamento passa da `_ensure_tenant_runtime_ready()` e dai bootstrap tenant gia' esistenti, non dalla singola route. In 2.216.0 il primo `/fascicoli/nuovo` post-restart ha mostrato richieste lente su `/api/time-tracking/active`, `/api/notifications` e `/api/v1/ui/fascicoli/nuovo` intorno a 65s. | Percorsi caldi verificati: `/documenti` 2.215.7 desktop 352.9 ms, tablet 210.8 ms, mobile 167.9 ms; `/fascicoli/nuovo` 2.216.0 desktop 761.3/647.2/538.7 ms, tablet 692.5 ms, mobile 646.7 ms a contenuto visibile. Non ripetere diagnosi generica su readiness; profilare legacy bootstrap/reconcile/module bootstrap con worker concorrenti. |
| PCT / CodiceOggetto PST ufficiale | Catalogo XSD + file Excel utente | Risolto e verificato in 2.215.6 | Il catalogo seed era utile ma non completo: il foglio Excel aveva 724 codici oggetto, 17 non presenti negli XSD attivi e mancavano 311 codici ufficiali estratti dal PST. | Importato catalogo tecnico dagli XSD ufficiali attivi con 1.018 codici unici; il foglio Excel resta supporto UI per aree/gruppi, mentre validazione e deposito usano solo la whitelist ministeriale. |
| Email PEC/ordinaria / allegati React | Visualizzazione allegati senza download | Risolto e verificato in 2.215.5 | Nel dettaglio messaggio erano visibili solo `Apri` e `Scarica`, senza un'azione esplicita per visualizzare l'allegato inline senza scaricarlo. | Aggiunta azione `Visualizza` in nuova scheda usando il link inline, con typecheck, contratti React e test mirati PEC/ordinaria verdi. |
| CI / Pytest core fase 7/10 observability parte 3/3 | `tests/test_web_bootstrap.py::test_docker_compose_prevede_runtime_ollama_sulla_stessa_macchina` | Risolto e verificato localmente in 2.215.3 | Il contratto CI cercava `PCT_EMAIL_ORDINARIA_DB=/data/email/ordinaria.json` nel `Dockerfile`, ma il path runtime ordinaria non era esplicitato nel layer immagine. | Ripristinata la variabile nel `Dockerfile`; test mirato e sotto-shard `run_pytest_phases.py --core-shard 7 --core-subshard 3` passati localmente. |
| CI / Coverage moduli critici | `Coverage moduli critici` aggregato GitHub | Risolto localmente, da confermare sul prossimo push | Tutti gli shard coverage erano verdi, ma il report aggregato era al 68% contro soglia 71 perche' alcuni test Lex gia' esistenti non facevano parte della suite `coverage-critical` pur coprendo moduli conteggiati dal gate. | Aggiunti `tests/test_lex_professional_upgrade.py` e `tests/test_lex_sources_and_studio_data.py` alla suite `coverage-critical`; gate locale con `--cov-fail-under=71` verde al 71.56%, senza abbassare soglie. |
| Email ordinaria / cancellazione multipla | `POST /api/v1/ui/email-ordinaria/bulk-action` con selezione ampia | Risolto e verificato in hotfix 2.215.2 | Una selezione molto ampia poteva non completare perche' il backend applicava i metodi singoli e salvava la casella email una volta per messaggio. | Introdotte operazioni bulk reali in `GestioneEmailRicevute` con un solo caricamento/salvataggio, audit aggregato e test anti-regressione sul salvataggio singolo; shard `email_ordinaria_react_bulk_action`, `fallback_globale_senza_tenant` e `bulk_action` verdi. |
| Gate React / Tariffario sticky | `npm --prefix frontend run test` | Risolto il 2026-05-10 | Il controllo `frontend/scripts/check-react-contracts.mjs` falliva su un'asserzione storica (`iu-tar-summary-sticky`) del Tariffario, non piu' presente nel wrapper reale della sidebar sticky. | Ripristinato il marker `iu-tar-summary-sticky` nel dock del riepilogo e aggiunto il selettore CSS dedicato; `check-react-contracts.mjs` e lo shard frontend `contratti` sono tornati verdi. |
| React/PCT 2.215.4 / CodiceOggetto PST | `tests/test_react_preventivo_wizard_console.py`, `tests/test_deposito_guidato.py`, `tests/test_practice_engine_validators.py` | Nessuna issue aperta | La criticita' emersa era architetturale: la tipologia tariffaria del wizard non puo' sostituire il CodiceOggetto ministeriale. | Aggiunto catalogo PST versionato, selezione governata nel wizard, propagazione a preventivo/conferimento/fascicolo e blocco predeposito se il codice manca o non e' ufficiale. |
| Gate React / whitelist fiscale governata | `node frontend/scripts/check-react-contracts.mjs` | Risolto il 2026-05-10 | Il contratto statico bloccava anche `FatturazionePage` e `TariffarioPage`, che per prodotto espongono un riepilogo fiscale governato e non un motore arbitrario lato client. | Aggiornata la whitelist mirata dei componenti autorizzati al preview fiscale governato (`FatturazionePage`, `fatturazioneData`, `TariffarioPage`, `tariffarioData`) senza allentare i controlli sulle altre superfici React. |
| CI / Lint + syntax | `python -m ruff check --output-format=github --select E9,F63,F7,F82 ...` | Risolto il 2026-05-10 | Il workflow GitHub sul commit `46116f37` falliva in `Ruff` non per i file bootstrap appena corretti, ma per `F823 Local variable find_cliente referenced before assignment` in `lex/providers/deterministic_provider.py`. | Rifattorizzato il fallback di `_studio_data_lookup_text`: fast-path con `build_cliente_answer(find_cliente(...))`, fallback ricco separato con alias `search_cliente` e riferimento facoltativo a `find_fascicoli_by_cliente`. Gate `ruff` completo e test Lex mirati tornati verdi. |
| CI / Ruff governed modules | `python -m ruff check --config pyproject.toml ... lex/retrieval ...` | Risolto il 2026-05-10 | Sul commit `65dbfe44` il gate generale `Ruff` era verde, ma `Ruff governed modules` falliva per `F841` su `lex/context/studio_context.py` e `lex/retrieval/orchestrator.py`, piu' `I001`/`UP017` nei moduli `lex/retrieval/*`. | Rimossi gli assegnamenti inutilizzati nel lookup dati studio e nell'orchestrator, applicato auto-fix sicuro Ruff per import ordering e `datetime.UTC`, poi verificato il gate governato e i test Lex mirati. |
| React Sito Studio / Contatti | Browser reale `http://localhost:8080/sito-studio/contatti` | Risolto in hotfix 2026-05-09 | Con zero contatti e zero prenotazioni la pagina veniva fermata dallo stato vuoto globale, mostrando "Nessun contatto o prenotazione" e testo da repository invece della dashboard operativa. | La pagina ora renderizza sempre la superficie React quando il payload e' valido: ingressi pubblici, modulo contatti, prenotazione, pannelli contatti/prenotazioni e stati vuoti specifici; rimossi testi tecnici visibili. |
| React nav / sezioni sidebar | Browser reale `Studio -> Statistiche -> Fascicoli` | Risolto in hotfix 2026-05-09 | La sidebar poteva accumulare o perdere sezioni aperte, rendendo poco stabile la navigazione tra cartelle operative. | La sidebar mantiene una sola cartella aperta: la sezione attiva resta aperta durante la navigazione interna e viene sostituita solo quando l'utente apre un'altra cartella. |
| React Impostazioni / AI Locale PC locale | `tests/test_impostazioni_ai_locale_react.py` | Risolto in hotfix 2026-05-09 | La pagina React aveva gia' il client `localAi.ts`, ma serviva un presidio esplicito per evitare che vecchi asset o chiamate residue trattassero lo stato/preparazione AI come controllo solo server/cloud. | Aggiunti `react-ai-local-guard.js`, caricamento dalla shell React, regole in `AGENTS.md`/design system e test mirati: AI Locale passa da IUSENTRA Local Signer sul PC, offre `Prepara AI locale` e mantiene scelta modelli automatica. |
| React full / pagine operative richieste | `node scripts/react-migration/check-full-react-route-contract.mjs` | Risolto in tranche 2.213.0 | Alcune superfici gia' dichiarate React full avevano ancora form HTML POST o testi visibili tecnici, quindi il risultato non era abbastanza end-to-end per l'utente. | Aggiunto `JsonPostForm`, convertiti i submit operativi principali, aggiunte risposte JSON nei blueprint collegati, ripuliti testi visibili e aggiornati manifest/gate/report. |
| React UI / testi visibili per avvocato | Browser desktop/mobile e guardia testo 2.214.0 | Risolto in tranche 2.214.0 | Lo studio poteva vedere diciture da sviluppatore o contratti interni come `Impeccable / Open Design`, `Dati applicativi`, `React`, `Flask`, `payload`, `runtime`, `provider`, `webhook`, `undefined`, `null`, `demo`, `sample` o `repository`. | Aggiunta guardia globale in React e nei template Flask, ripuliti bridge/componenti, portati i dettagli email in React e verificato browser Docker 2.214.0 desktop/mobile sulle pagine richieste senza testi tecnici visibili. |
| React Impostazioni / Backup in tab | `tests/test_react_shell.py::test_impostazioni_react_api_redige_segreti_e_salva_configurazioni` | Risolto in questa tranche | Il bridge Impostazioni importava `get_backup` da `web.helpers`, dove non esiste; il salvataggio PEC rilanciava il payload completo e falliva con `ImportError`. | `react_impostazioni_bridge.py` ora usa il loader tenant-aware da `current_app.extensions["core_runtime"]` per Backup, con fallback controllato; shard mirato e shard route Impostazioni passati. |
| React Impostazioni / PEC invio locale | `tests/test_impostazioni_pec_local_signer_react.py` | Risolto in hotfix 2026-05-09 | Regressione: `Verifica invio PEC` in React poteva chiamare il test SMTP applicativo invece del Local Signer sul PC in uso. | `SettingsActions.tsx` ora chiama `testPecSmtpViaLocalSigner`, la shell React intercetta il vecchio endpoint verso `/pec/smtp/test`, il bridge API rifiuta il test SMTP server-side e i test mirati sono verdi. |
| React Impostazioni / AI Locale sul PC | `tests/test_impostazioni_ai_locale_react.py` | Risolto in hotfix 2026-05-09 | Regressione UI: la tab AI Locale non rendeva chiaro cosa fare se Ollama/modelli mancavano e poteva affidarsi al controllo applicativo invece del PC dello studio. | `SettingsActions.tsx` usa Local Signer per verifica/preparazione AI, `localAi.ts` invia `/ai/status` e `/ai/bootstrap` con modello automatico, `react-ai-local-guard.js` protegge gli asset gia' compilati, `AGENTS.md` e design system fissano la regola. |
| Hetzner backup / Ollama rigenerabile | `tests/test_hetzner_backup_retention.py` | Risolto in hotfix 2026-05-09 | Il backup escludeva solo `./ollama`, ma un controllo reale sull'archivio Hetzner ha trovato ancora cartelle `intelligence/downloads/ollama` e tenant analoghe. | `backup.sh` aggiunge esclusioni obbligatorie per root, download globali e download tenant, verifica il contenuto dell'archivio e fallisce se trova ancora percorsi `ollama`; test runtime crea un backup reale temporaneo e controlla che Ollama non sia archiviato. |
| React 2.210.0 / Controlli Atti e Strumenti | Visual smoke Chrome desktop/tablet/mobile | Risolto in questa tranche | I primi smoke usavano `networkidle` e un data root non abbastanza isolato, con rischio di toccare JSON runtime del tenant locale; inoltre la UI telematica mostrava `payload` e `backend` in testi visibili. | Runtime locale isolato con `PCT_DATA_ROOT` temporaneo e server threaded; ripuliti i JSON runtime toccati; titolo `/deposito/checklist` allineato a `Controlli Atti`; testi visibili ripuliti; smoke finale verde su desktop/tablet/mobile. |
| React gate / anti-finto | `node scripts/react-migration/check-no-fake-react-full.mjs` | Risolto in questa tranche | Il guardrail considerava ancora `/sincronizzazione-calendari` come legacy, ma la tranche corrente lo integra nella pagina Impostazioni React. | Aggiornato il check: `/sincronizzazione-calendari`, `/impostazioni/calendario` e `/backup` devono essere `react_operational_full` sbloccati; comando rilanciato e verde. |
| Docker locale / runtime dati | `docker compose up -d --no-build redis app nginx` dopo build `--no-cache` | Risolto in questa tranche | Il container app avviato dalla build 2.208.0 falliva con `PermissionError: email/ordinaria.json`: la posta ordinaria non aveva un env Docker locale esplicito e il fallback finiva nel path repository invece che nel volume `/data`. | Aggiunto `PCT_EMAIL_ORDINARIA_DB=/data/email/ordinaria.json` al `Dockerfile`; `core_runtime` ora deriva il fallback ordinaria dal path PEC o da `PCT_DATA_ROOT`; aggiunto test anti-regressione in `tests/test_web_bootstrap.py`. Non ricreare piu' `email/ordinaria.json` nel repository. |
| Docker locale / healthcheck | `/api/pronto` dopo fix posta ordinaria | Risolto in questa tranche | Il container non rispondeva al readiness probe perche' lo startup web restava occupato in `GestioneTenant.sync_user_directory()` -> `reconcile_storage_aliases()` -> scansioni/merge directory tenant. | `sync_user_directory` ora accetta `reconcile_storage=False`; lo startup web lo usa per non fare reconcile pesanti nel worker HTTP. Il reconcile completo resta nei flussi admin espliciti. |
| Stato corrente | Coda immediata React Full / Lex AI / gate finali | Nessuna issue aperta immediata | Tutte le failure reali incontrate nella coda `07-lex-ai` sono state corrette e verificate; i timeout larghi sono stati isolati in sotto-shard singoli. | Prossimo lavoro: solo eventuali nuove failure introdotte da modifiche successive o da CI esterna. |
| React shell / Impostazioni full React | `python -m pytest -q tests/test_react_shell.py ...` completo monolitico | Isolato come timeout di batch | Il file intero supera il budget operativo da 5 minuti quando lanciato tutto insieme; i test toccati dalla tranche Impostazioni passano in shard mirati. | Non rilanciare il file completo per questa tranche. Usare gli shard registrati in `pytest-confirmed-ok.md` e solo eventuali failure nuove. |
| Docker locale / reattivita' app | log Gunicorn e navigazione locale | Risolto in questa tranche | Il Docker locale avviava l'app con un solo processo web: una richiesta lunga poteva rendere lenta tutta la UI e generare timeout del worker. | Aggiunta capacita' web esplicita in `docker-compose.yml` (`WEB_CONCURRENCY`, thread e connessioni); aggiunto contratto in `tests/test_packaging_consistency.py`. Verifiche Docker finali da registrare in `pytest-confirmed-ok.md` dopo rebuild no-cache. |
| Misc / email / Lex | `tests/test_email_client.py::test_base_template_non_renderizza_vecchio_lex_duplicato` | Risolto, test mirato OK | Il test pretendeva ancora il marker `__legacy_lex_disabled__`, mentre il contratto React aggiornato richiede che quel marker non sia piu' presente nel template base. | Aggiornato il test legacy: verifica assenza del marker e presenza di un solo mount governato `{% include "components/pct_ai_widget.html" %}`. |
| Documenti AI | `tests/test_document_intelligence_repository_sql.py::test_document_ai_repository_sqlite_persistente_filtra_e_ricarica` | Risolto, test mirato OK | `TypeError: isinstance() arg 2 must be a type`: in ambiente test lo stub di `PostgresRepositoryBackend` puo' essere `None`, mentre `_detect_backend` lo usava direttamente in `isinstance`. | Hardened `DocumentAIRepository`: `isinstance` viene usato solo se `PostgresRepositoryBackend` e' davvero un tipo; `from_postgres_dsn` fallisce in modo esplicito se il backend non e' disponibile. |
| Flask core / deploy Hetzner | `tests/test_web_bootstrap.py::test_docker_compose_hetzner_allinea_email_ordinaria_e_ai_locale` | Risolto, test mirato OK | Il compose Hetzner usava default `http://ollama:11434/api`; il contratto chiede `http://ollama:11434/api/version`. | Corretto `deploy/hetzner/docker-compose.hetzner.yml`. Resta da completare solo il tail della fase `01-flask-core`. |
| Storage / coverage SQL | `tests/test_repository_sql_parity.py::test_coverage_surface_usa_database_tenant_postgresql_come_fallback` | Risolto, test mirato OK | Lo stub test di `resolve_runtime_postgres_dsn` restituiva sempre `None`, impedendo a `build_repository` di usare `TENANT_DATABASE_CONFIG` PostgreSQL come fallback. | Corretto `tests/conftest.py`: lo stub ora usa explicit DSN, chiavi env passate nella config e `database_config_to_dsn` su database o `TENANT_DATABASE_CONFIG`. Resta da completare il tail della fase `04-storage` dal batch successivo. |
| React UI / preventivi | `02-react-ui` item 30-39 con `tests/test_react_preventivo_wizard_console.py::*` | Risolto come timeout di batch; item singoli OK | Il batch da 10 item ha superato 300s; i singoli item hanno confermato che non era una failure funzionale. | Registrati gli item OK in `pytest-confirmed-ok.md`. Riprendere la fase `02-react-ui` da item 40. |
| React Tariffario / riepilogo sticky | Browser smoke `/tariffario` dopo scroll | Risolto in 2.214.1 | Il riepilogo in tempo reale era sticky dentro una colonna laterale troppo corta: il browser lo lasciava scorrere via appena finiva il contenitore, quindi in pagina restava visibile il supporto normativo ma non il riepilogo. | La colonna laterale desktop e' ora sticky e scrollabile, con il riepilogo in cima; sotto 1040px torna statica. Test statico e smoke browser desktop/tablet/mobile confermati. |
| React shell / legacy-first | `tests/test_react_shell.py::test_blocco_telematico_studio_admin_resta_legacy_first` | Risolto, test mirato OK | Il gate legacy-first includeva `/strumenti-operativi`, ma `_LEGACY_FIRST_PREFIXES` della shell React non lo includeva. | Aggiunto `/strumenti-operativi` a `web/blueprints/react_shell.py`; test mirato e gli altri quattro test del batch passati. |
| React route gate / legacy-first | `tests/test_react_shell.py::test_route_gate_non_promuove_moduli_studio_telematico_admin_incompleti` | Risolto, test mirato OK | `_REACT_PREFIXES` e `_REACT_EXACT` includevano `/strumenti-operativi`, ma `_LEGACY_OPERATIONAL_PREFIXES` non lo escludeva dalla promozione React. | Aggiunto `/strumenti-operativi` a `_LEGACY_OPERATIONAL_PREFIXES` in `web/bootstrap/react_route_gate.py`. |
| Flask core / Local AI cloud runtime | `tests/test_web_bootstrap.py::test_runtime_cloud_hosted_ignora_percorsi_ai_del_tenant` | Risolto, test mirato OK | Lo stub `LocalAIService` di test accettava il costruttore ma lasciava `db_path` stringa, mentre il servizio reale espone `Path`. | Aggiornato lo stub in `tests/conftest.py` per convertire i path principali in `Path`. |
| Documenti / Editor AI repository | `tests/test_editor_ai_repository.py::test_repository_sqlite_filtra_tenant_fascicolo_e_versiona`, `tests/test_editor_ai_repository.py::test_repository_sqlite_modifiche_e_audit` | Risolto, test mirati OK | Stesso problema gia' corretto su Document AI: `PostgresRepositoryBackend` puo' essere `None` nello stub, ma veniva usato direttamente in `isinstance`. | Hardened `EditorAIRepository` con guard tipizzato e errore esplicito in `from_postgres_dsn` se il backend PostgreSQL non e' disponibile. |
| Flask core / Lex prompt | `tests/test_web_bootstrap.py::test_contesto_lex_compatta_le_sezioni_e_limita_le_fonti` | Risolto, test mirato OK | Il contratto si aspettava il guardrail testuale `Se c'e' gia' una richiesta operativa o di ricerca, non aprire con saluti`, ma il prompt aveva solo una formula equivalente. | Reinserita la frase esatta in `lex/prompts/prompt_builder.py` dentro la voce di Lex. |
| React shell / Sito Studio | `tests/test_react_shell.py::test_react_migration_matrice_completa_route_api_e_card_operative` | Risolto, test mirato OK | `/sito-studio/` veniva esclusa dal gate React per la regola generica sui percorsi profondi del sito studio. | Aggiornato `register_react_route_gate`: la root `/sito-studio/` passa a React, mentre builder e percorsi profondi restano legacy-first. |
| Core business / Sito Studio seed | `tests/test_studio_site.py` blocco seed/flag/contatti/prenotazioni/admin | Risolto, test mirati OK | Il passaggio di `/sito-studio/` alla shell React saltava il side-effect storico di bootstrap sito, quindi i test dominio non trovavano il sito inizializzato. | Il gate React ora chiama `ensure_current_studio_site()` per `/sito-studio` e `/sito-studio/contatti`; la shell riceve anche il testo bootstrap "Sito Studio" per compatibilita' contrattuale. |
| Core business / workflow commerciale-onboarding | `03-core-business` item 440-444 | Risolto come timeout di batch; item singoli OK | Il batch da cinque test workflow ha superato 300s, ma tutti gli item passano singolarmente. | Registrati gli item OK in `pytest-confirmed-ok.md`; riprendere `03-core-business` da item 445. |
| Multi-studio / percorsi repository sensibili | `tests/test_storage_strategy.py -k "sensitive_repositories or tenant_context_is_missing or token_dir_uses_tenant_agenda_path or tenant_preventivi_repository"` | Risolto il 2026-05-10 | Alcuni loader e helper laterali usavano ancora path globali `app.config[...]` per backup, soggetti, indice ricerca, privacy, condivisioni, calendario impostazioni, preventivi e sync PDP/PEC, con rischio di letture cross-studio quando il tenant attivo non coincideva con la root globale. | `web/services/core_runtime.py` ora usa `_cfg_data_path(...)` per tutti i repository sensibili; chiusi anche i residui in `react_impostazioni_calendar.py`, `search_routes.py`, `fascicoli_editor_routes.py`, `template_atti.py` e `pdp_penale_runtime.py`. Test mirati 6/6 verdi e documentati in `pytest-confirmed-ok.md`. |
| Multi-studio / superfici admin dati | `tests/test_storage_strategy.py -k "admin_database_react_payload_uses_tenant_backup_dir or admin_shared_helpers_use_tenant_paths"` | Risolto il 2026-05-10 | La superficie `admin/database` e alcuni helper admin condivisi potevano ancora leggere `BACKUP_DIR`, configurazione studio e manager dati dalla root globale invece che dal tenant attivo. | `web/bootstrap/admin_database_routes.py`, `web/blueprints/api_v1_react.py`, `web/services/admin_surfaces_shared.py` e `web/services/system_health_surface.py` usano ora i path tenant-aware; test mirati verdi e documentati in `pytest-confirmed-ok.md`. |
| Multi-studio / helper satellite operativi | `tests/test_storage_strategy.py -k "topbar_cfg_value_blocks_when_tenant_context_is_missing or template_blueprint_uses_tenant_template_paths or applicazioni_runtime_uses_tenant_template_and_portale_paths or legal_intelligence_uses_tenant_daily_and_portale_paths"` | Risolto il 2026-05-11 | Alcuni helper locali ancora esposti (`topbar_operational`, `template_atti`, `applicazioni_runtime`, `legal_intelligence`) potevano riaprire fallback ai path globali per template, portali, daily DB o caselle operative. | Aggiunti helper tenant-aware/fail-closed nei moduli interessati; test mirati 10/10 verdi insieme al blocco core/admin e registrati in `pytest-confirmed-ok.md`. |
| Multi-studio / email ordinaria e PEC | `tests/test_email_client.py -k "fallback_globale_senza_tenant"` | Risolto il 2026-05-11 | Le API React e le route mail potevano ancora risolvere `EMAIL_CASELLA_DB`, `EMAIL_ORDINARIA_DB` e `MESSAGGI_DB` su fallback globali quando il multi-studio era attivo ma la request non aveva tenant valido; questo poteva esporre o modificare messaggi di un altro studio. | Aggiunto `web/services/tenant_paths.py` con `TenantDataPathError`; PEC, ordinaria, sync e bulk action ora falliscono chiusi senza tenant valido. Test mirati 5/5 e compileall verdi registrati in `pytest-confirmed-ok.md`. |
| Lex AI / giurisprudenza specifica | `lex/tests/unit/test_bundle_scenarios.py::test_lookup_sentenza_con_numero_pdf_degrada_senza_riferimenti_verificati` | Risolto, test mirato OK | La risposta per sentenza specifica esponeva `workflow=giurisprudenza_specifica` nel metadata pubblico e perdeva il coverage gap atteso. | Il metadata pubblico torna `giurisprudenza`, il dettaglio resta in `workflow_detail=giurisprudenza_specifica`; coverage gap e frase prudenziale sono preservati. |
| React UI / Regia | `02-react-ui` item 120-124 (`test_regia_*`) | Risolto come timeout di batch; item singoli OK | Il batch da 5 item ha superato 300s per combinazione di test Regia lenti. | Tutti e cinque gli item sono passati singolarmente e sono registrati in `pytest-confirmed-ok.md`. |
| CI contracts | `tests/test_ci_no_regression_contract.py::test_pytest_core_uses_ten_parallel_shards_without_removing_tests` | Corretto e test mirato OK | `observability_runtime` e `ocr_worker` non cadevano piu' negli shard attesi dal contratto CI. | Corretto `scripts/run_pytest_phases.py`; test mirato passato. Resta da rilanciare la fase `00-ci-contracts` completa. |
| Storage migration | `tests/test_migration_assistant.py` due test PostgreSQL | Corretto e gruppo mirato OK | Lo stub test di `pct.postgres_runtime_support` esponeva `resolve_runtime_postgres_dsn` ma non `database_config_to_dsn`, causando `NoneType object is not callable`. | Corretto `tests/conftest.py`; gruppo mirato passato. Resta da rilanciare la fase `04-storage`. |
| Core business | `03-core-business` batch preventivi/conferimento | Isolato | Il batch combinato superava 300s; i test singoli del blocco sono passati. | Non rilanciare quel batch intero. Proseguire dalla coda residua della fase 03 con batch piu' piccoli o item singoli. |
| Lex AI / deposito telematico | `lex/tests/unit/test_bundle_scenarios.py::test_errore_telematico_restituisce_risposta_deterministica_con_metadati` | Risolto, test mirato OK | Il workflow interno `deposito_telematico` veniva normalizzato nel metadata pubblico come `telematico_status`, ma routing/provider/composer non lo trattavano ancora come percorso deterministico telematico. | Allineati `AnswerBuilder`, `ProfessionalAnswerComposer`, `DeterministicProvider` e `ProviderRegistry`: risposta con `Esito operativo governato`, provider deterministico e metadata pubblico `telematico_status`. |
| Lex AI / risposta professionale | `lex/tests/unit/test_professional_answer.py::test_risposta_fascicolo_viene_strutturata_con_qualita_e_azioni`, `lex/tests/unit/test_professional_answer.py::test_risposta_normativa_incompleta_richiede_revisione_professionale` | Risolto, test mirati OK | La nuova struttura a sezioni aveva sostituito headings contrattuali ancora richiesti: `Quadro verificato`, `Qualita della risposta`, `Risposta professionale`, `Limiti e verifiche`. | Ripristinate le intestazioni contrattuali in `ProfessionalAnswerComposer`, mantenendo la struttura professionale e le sezioni operative. |
| Lex AI / routing telematico | `lex/tests/unit/test_router.py::test_router_resolves_telematico_workflow` | Risolto, test mirato OK | `explain_telematico_error` veniva trattato come flusso completo `deposito_telematico`, mentre il contratto storico lo classifica come stato/errore `telematico_status`. | Separato il routing: `explain_telematico_error` -> `telematico_status`; `deposito_telematico` e query deposito restano `deposito_telematico`. Verificati anche i test Legal Studio correlati. |
| Lex AI / routing normativa telematica | `lex/tests/unit/test_router.py::test_router_infers_normativa_from_query_text` | Risolto, test mirato OK | La query "normativa vigente sul deposito telematico" veniva presa dal routing operativo telematico prima del routing normativo. | Aggiunta priorita' mirata: se la domanda e' normativa e riguarda il telematico, il workflow e' `normativa`; checklist e deposito operativo restano `deposito_telematico`. |
| Lex AI / follow-up lenti | `07-lex-ai` batch #14 da item 175 a 179 | Isolato, sotto-shard singoli OK | Il batch combinato ha superato 300 secondi, ma i cinque item passano singolarmente; tre test richiedono circa 2 minuti ciascuno. | Documentato come timeout largo isolato; non rilanciare il batch intero. |
| Lex AI / Local AI test stub | `tests/test_local_ai.py::test_local_ai_bootstrap_disabled_is_non_blocking` | Risolto, test mirato OK | Lo stub Lex in `tests/conftest.py` sostituiva `pct.local_ai` anche quando la suite Local AI richiedeva il servizio reale, causando assenza di `bootstrap_runtime()`. | Ora `pct.local_ai` viene importato realmente quando disponibile; lo stub resta solo fallback e include i metodi minimi non bloccanti. |
| Lex AI / Local AI API context lenti | `07-lex-ai` batch #4 da item 545 a 549 nella coda da 530 | Isolato, sotto-shard singoli OK | Il batch combinato ha superato 300 secondi; i cinque item passano singolarmente, con piu' test lenti tra 80 e 175 secondi. | Documentato come timeout largo isolato; non rilanciare il batch intero. |
| Lex AI / allegati citabili | `tests/test_local_ai.py::test_api_assistente_context_integra_documenti_caricati` | Risolto, test mirato OK | Gli allegati caricati venivano trasformati in evidenze ma non comparivano nelle `citations` del payload `/api/assistente/context`. | `build_bounded_http_payload` ora aggiunge le evidenze da allegati alle citazioni e alle sorgenti governate. |
| Lex AI | `07-lex-ai` fase corrente | Risolto, fase completa | Item 75-601 confermati con shard/sotto-shard; timeout larghi isolati e test lenti confermati singolarmente. | Non ripetere salvo modifiche collegate a Lex/Local AI/ricerca; usare i report JSON e questo storico. |

## Coda immediata consigliata

1. `03-core-business` e' completa; non ripetere salvo modifiche collegate.
2. Fase `07-lex-ai` completa; passare ai gate finali pertinenti.
3. Gate finali frontend/react eseguiti e verdi: `npm test`, `npm run typecheck`, `npm run build`, `run-full-react-migration`, `run-legal-ui-checks`, packaging/release readiness e visual smoke 2.210.0.
4. Gate Docker locale: non ripetere la diagnosi su `email/ordinaria.json`; se ricompare, verificare prima `PCT_EMAIL_ORDINARIA_DB` e `PCT_DATA_ROOT`.
5. Gate Docker locale: non ripetere la diagnosi su `/api/pronto` che resta occupato in `sync_user_directory`; verificare prima che lo startup usi `reconcile_storage=False`.
6. Aggiornare questo file con ogni failure reale e spostare in `pytest-confirmed-ok.md` i batch confermati.

## Note hotfix 2026-05-11

| Area | Gate | Stato | Nota |
| --- | --- | --- | --- |
| Notifiche legali parametriche 2.216.8 | Test mirati, contratti React, packaging, build e browser su bundle 2.216.8 registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta | Tutti i modelli richiesti sono nel catalogo JSON; la precompilazione usa dati reali IUSENTRA e lascia vuoti i dati non certi invece di inventarli. |
| Fascicoli / preventivi / wizard PST | Test mirati e browser reale registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta | Il codice `014001` digitato nel catalogo o nell'oggetto viene ora risolto dal catalogo PST ufficiale e arriva fino al fascicolo guidato con redirect a deposito assistito. |

## Note hotfix 2026-05-12

| Area | Gate | Stato | Nota |
| --- | --- | --- | --- |
| Notifiche legali modelli personalizzati 2.216.9 | Test mirati, contratti React, packaging, build, Docker locale e browser desktop/mobile registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta | L'avvocato puo' vedere il testo del modello relata, duplicarlo o crearne uno nuovo usando campi automatici IUSENTRA. `Deposito prova notifica` e `Comunica al cliente` leggono la pratica selezionata e precompilano i dati certi senza salvare backup o inventare RAC/RdAC. |

| Template Atti STRICT 2.218.1 | Inventario e test mirati registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta sull'inventario numerico | L'inventario riconcilia 1320 template canonici su 1320 attesi e mantiene 4576 record fonte con 3256 copie eccedenti tracciate. Zero template canonici senza Cartabia, prefill, timbro, renderer o binding dopo recupero interno; le copie fonte riconciliabili restano tracciate come evidenza, non bloccano i 1320 modelli canonici. La prontezza del modello non dipende dall'esistenza preventiva di un fascicolo: cliente/pratica vengono richiesti e risolti quando l'avvocato avvia la compilazione. |
| Template Atti STRICT autore/editor 2.218.1 | Batch unico template/editor | Timeout isolato | Il comando unico con 12 file test ha superato il budget locale da 240s senza produrre failure funzionali. | Gli stessi file sono stati verificati in tre shard: 11/11, 10/10 e 24/24 passati. `npm --prefix frontend run typecheck` verde. |

## Note hotfix 2026-05-13

| Area | Gate | Stato | Nota | Azione |
| --- | --- | --- | --- | --- |
| PWA/Web Push configurazione operativa 2.218.4 | Test mirati, script, typecheck, build, packaging e readiness registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta | La causa del messaggio `Da configurare` e' ora diagnosticata da `/api/push/public-key`; generatore VAPID, diagnostica CLI e script Hetzner non salvano chiavi nel repository e non stampano la private key nei log normali. Su richiesta corrente non sono stati creati backup. | Non rilanciare shard Web Push salvo modifiche collegate a `pct/notifications`, `push_notifications`, `frontend/src/lib/pushNotifications.ts`, pannello Notifiche o script Hetzner. |
| Template Atti compilatore React 2.218.2 | Pytest mirati, typecheck, build e browser Playwright registrati in `pytest-confirmed-ok.md` | Nessuna issue aperta | La compilazione `/template-atti/compila/<codice>` e' ora servita dalla shell React e non dal vecchio compilatore Jinja, salvo fallback tecnico `_legacy=1`. Le note dei campi sono italiane e leggibili; i blocchi restano sui dati concreti mancanti dell'atto, non sulla disponibilita' preventiva di un fascicolo generico. | Non rilanciare shard gia' verdi salvo modifiche collegate a Template Atti, API React, route gate o asset Vite. |
| Multi-studio hardening 2.218.3 | `python -m pytest tests -q` | Timeout di suite monolitica | Il comando completo ha superato 15 minuti (`timeout 904s`) senza restituire report finale. Non e' stato dichiarato verde. | Usare i gate mirati registrati in `pytest-confirmed-ok.md`; rilanciare la suite completa solo in CI o con shard/fasi esistenti. |
